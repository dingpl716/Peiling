package string_array;

//	sort the array, so that all negtive numbers are ahead of positive numbers
//	the relenvence order between two numbers should not be changed
//	do it in O(n) time and O(1) space

public class SortNegtiveAndPostive {
	
	public void sort(int[] A) {
		
	}
}
